  <div class="col-sm-2 col-lg-4 product-display">
    <a><h3><%= link_to product.name, product %></h3></a>
    <p><%= link_to image_tag(product.image_url, class: "img-responsive"), product %></p>
    <p><%= product.description %></p>
    <p><%= product.colour %></p>
    <p>£<%= product.price %></p>
    <p><%= link_to 'Show', product %>
    <%= link_to 'Edit', edit_product_path(product) %>
    <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>
  </div>