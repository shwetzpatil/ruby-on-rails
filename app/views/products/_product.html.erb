  <div class="col-sm-2 col-lg-4 product-display">
    <a><h3><%= link_to product.name, product %></h3></a>
    <p style="display: inline-block"><%= link_to image_tag(product.image_url, class: "img-responsive", size: "300"), product %></p>
    <p><%= logger.debug "#{product.description}" %></p>
    <p><%= logger.debug "#{product.colour}" %></p>
    <p>£<%= logger.debug "#{product.price}" %></p>
    <p>
    <% if can? :crud, Product %>
      <%= link_to 'Edit', edit_product_path(product), class: "btn btn-sm btn-success" %>
      <%= link_to 'Destroy', product, class: "btn btn-sm btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></p>
    <% end %>
  </div>